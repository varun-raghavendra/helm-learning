apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lichen_algae.fullname" . }}-test-connection"
  labels:
    {{- include "lichen_algae.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lichen_algae.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
